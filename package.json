{
  "name": "roster",
  "private": true,
  "version": "1.5.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "tauri": "tauri",
    "dev": "tauri dev --config src-tauri/tauri.debug.conf.json",
    "build:dev": "tauri build --debug --bundles app --config src-tauri/tauri.debug.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@highlightjs/cdn-assets": "^11.11.1",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "dompurify": "^3.4.7",
    "marked": "^18.0.4"
  }
}
